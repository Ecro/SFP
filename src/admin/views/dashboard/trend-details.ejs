<%- include('../partials/header', { title }) %>
<%- include('../partials/sidebar', { title }) %>
                <a href="/admin/trends" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Back to Trends
                </a>

                <div class="row">
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h5>Trend Run Details</h5>
                            </div>
                            <div class="card-body">
                                <table class="table">
                                    <tr><td>Status:</td><td><%= trend.status %></td></tr>
                                    <tr><td>Selected Topic:</td><td><%= trend.selected_topic || 'None' %></td></tr>
                                    <tr><td>Topics Found:</td><td><%= trend.topics_found || 0 %></td></tr>
                                    <tr><td>Execution Time:</td><td><%= trend.execution_time_ms ? (trend.execution_time_ms/1000).toFixed(1) + 's' : '-' %></td></tr>
                                    <tr><td>Timestamp:</td><td><%= new Date(trend.timestamp || '').toLocaleString() %></td></tr>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h5>Discovered Topics</h5>
                            </div>
                            <div class="card-body">
                                <% if (topics.length > 0) { %>
                                    <% topics.forEach(function(topic, index) { %>
                                        <div class="mb-3 p-2 border rounded">
                                            <div class="fw-bold">#<%= index + 1 %> <%= topic.keyword %></div>
                                            <small class="text-muted">
                                                <%= topic.category %> • 
                                                <%= parseInt(topic.predicted_views).toLocaleString() %> predicted views
                                            </small>
                                            <div class="progress mt-1" style="height: 5px;">
                                                <div class="progress-bar" style="width: <%= (topic.score || 0) %>%"></div>
                                            </div>
                                        </div>
                                    <% }); %>
                                <% } else { %>
                                    <p class="text-muted">No topics discovered in this run.</p>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>
<%- include('../partials/footer') %>
