<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - SFP Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <nav class="col-md-3 col-lg-2 d-md-block sidebar collapse" style="min-height: 100vh; background-color: #2c3e50;">
                <div class="position-sticky pt-3">
                    <div class="text-center mb-4">
                        <h4 class="text-white">SFP Admin</h4>
                        <small class="text-muted">Short-Form Publisher</small>
                    </div>
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link" href="/admin" style="color: #ecf0f1;">
                                <i class="bi bi-speedometer2"></i> Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/trends" style="color: #ecf0f1;">
                                <i class="bi bi-graph-up"></i> Trend Analysis
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="/admin/settings" style="color: #ffffff; background-color: #3498db;">
                                <i class="bi bi-gear"></i> Settings
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>

            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2"><%= title %></h1>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h5>Environment Configuration</h5>
                            </div>
                            <div class="card-body">
                                <table class="table">
                                    <tr><td>Node Environment:</td><td><%= env.NODE_ENV %></td></tr>
                                    <tr><td>Log Level:</td><td><%= env.LOG_LEVEL %></td></tr>
                                    <tr><td>Database Path:</td><td><%= env.DB_PATH %></td></tr>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h5>System Information</h5>
                            </div>
                            <div class="card-body">
                                <table class="table">
                                    <tr><td>Node Version:</td><td><%= process.version %></td></tr>
                                    <tr><td>Platform:</td><td><%= process.platform %></td></tr>
                                    <tr><td>Uptime:</td><td><%= Math.round(process.uptime() / 60) %> minutes</td></tr>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row mt-4">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <h5>API Configuration Status</h5>
                            </div>
                            <div class="card-body">
                                <div class="alert alert-info">
                                    <h6>Required Environment Variables:</h6>
                                    <ul>
                                        <li><code>ANTHROPIC_API_KEY</code> - Claude API for script generation</li>
                                        <li><code>ELEVENLABS_API_KEY</code> - Text-to-speech narration</li>
                                        <li><code>LUMA_API_KEY</code> - Video synthesis</li>
                                        <li><code>YOUTUBE_API_KEY</code> - YouTube upload integration</li>
                                        <li><code>SLACK_WEBHOOK_URL</code> - Error notifications</li>
                                    </ul>
                                    <p class="mb-0">Add these to your <code>.env</code> file based on <code>.env.example</code></p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>